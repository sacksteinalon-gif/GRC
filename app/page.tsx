import Link from 'next/link';import { frameworks, modules, stats } from '@/lib/data';import { StatCard, StatCardGrid } from '@/components/grc/primitives';
export default async function Landing(){const s=stats();return <main className="min-h-screen bg-slate-950 text-white"><section className="mx-auto max-w-7xl px-6 py-20"><p className="text-purple-300">Regulated fintech control tower</p><h1 className="mt-4 max-w-4xl text-6xl font-black">GRC Platform for UK/EU operational resilience, DORA, FCA-OR, ISO 27001 and PCI-DSS.</h1><Link href="/login" className="mt-8 inline-block rounded-xl bg-purple-600 px-5 py-3 font-bold">Demo login</Link><div className="mt-12 text-slate-900"><StatCardGrid>{Object.entries(s).map(([k,v])=><StatCard key={k} label={k} value={v}/>)}</StatCardGrid></div><div className="mt-12 grid gap-4 md:grid-cols-5">{Object.entries(modules).map(([g,items])=><div className="rounded-xl border border-slate-800 p-4" key={g}><h3 className="font-bold text-purple-200">{g}</h3>{items.map(i=><Link className="mt-2 block text-slate-300" href={'/'+i.toLowerCase().replaceAll(' ','-')} key={i}>{i}</Link>)}</div>)}</div><div className="mt-10 flex gap-2">{frameworks.map(f=><span className="rounded-full bg-white/10 px-3 py-1" key={f}>{f}</span>)}</div></section></main>}
