import { Shell } from '@/components/layout';import { SectionCard } from '@/components/grc/primitives';
export default async function Detail({params}:{params:Promise<{id:string}>}){const {id}=await params;return <Shell><div className='space-y-4'><h1 className='text-3xl font-black'>Incident Register detail: {id}</h1><SectionCard title='Cross-linked entities'>Controls, incidents, issues, requirements and vendors are shown as linked tabs in the production workflow.</SectionCard><SectionCard title='Workflow timeline'>Created → Assessed → Reviewed → Approved.</SectionCard><SectionCard title='Audit trail'>AuditLog and polymorphic comments capture all mutations.</SectionCard></div></Shell>}