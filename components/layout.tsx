import Link from 'next/link';import { modules } from '@/lib/data';
const href=(m:string)=>'/' + m.toLowerCase().replaceAll(' ','-');
export function Shell({children}:{children:React.ReactNode}){return <div className="min-h-screen md:flex"><aside className="bg-slate-950 p-5 text-white md:w-72"><Link href="/dashboard" className="text-2xl font-black text-purple-300">GRC Platform</Link>{Object.entries(modules).map(([g,items])=><div key={g} className="mt-6"><p className="mb-2 text-xs uppercase text-slate-400">{g}</p>{items.map(i=><Link key={i} href={href(i)} className="block rounded-lg px-3 py-2 text-sm hover:bg-slate-800">{i}</Link>)}</div>)}</aside><main className="flex-1"><header className="sticky top-0 z-10 flex items-center justify-between border-b bg-white/80 p-4 backdrop-blur dark:bg-slate-950/80"><span className="font-semibold">Home / Workspace</span><span className="rounded-full bg-purple-100 px-3 py-1 text-sm text-purple-700">admin@rapyd.net</span></header><div className="p-6">{children}</div></main></div>}
